define('sql_engine/parserCore', ['sql_engine/Pattern'], function(Pattern){

    return {
        txt: function(str, pos){
            return new Pattern(function(str,pos){

            })
        }
    }
});